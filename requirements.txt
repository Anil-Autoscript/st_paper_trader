# ST Paper Trader — Python dependencies
# Python 3.12+ required (GitHub Actions ubuntu-latest default)
#
# Install:
#   pip install --pre pandas-ta
#   pip install -r requirements.txt
#
# NOTE: pandas-ta must be installed with --pre flag (beta build).
#       The GitHub Actions workflows handle this automatically.

# Kite Connect
kiteconnect>=5.0.0

# Technical analysis — pandas-ta 0.4.x beta (only version supporting Python 3.12)
# Install separately with: pip install --pre pandas-ta
# Listed here so pip freeze/audit works; workflows install it with --pre
pandas-ta>=0.4.0b0

# Core data
pandas>=2.0.0
numpy>=1.24.0

# HTTP
requests>=2.31.0

# TOTP for automated Kite login
pyotp>=2.9.0

# Browser automation for TOTP login flow
mechanize>=0.4.9

# GitHub secret encryption (PyNaCl) — for kite_login.py auto-updater
PyNaCl>=1.5.0

# Timezone support
tzdata>=2024.1

# Excel reading
openpyxl>=3.1.0
python-calamine>=0.1.7

# Progress bar
tqdm>=4.66.0
